### Installer

Package to perform various installation automations.
Created by using:

- typescript
- google zx

### Install

```bash
sudo npm i -g zx
sudo npm i -g @aibulat/installer
```

### Use

On Ubuntu Linux:

```bash
sudo i-ubuntu-mysql
sudo i-ubuntu-vim
```
